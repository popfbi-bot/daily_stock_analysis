================================================================
① 这次到底跑的是哪个仓库
================================================================
仓库        : popfbi-bot/daily_stock_analysis
触发事件    : push
分支 / ref  : refs/heads/main
workflow    : popfbi-bot/daily_stock_analysis/.github/workflows/96-notify-test.yml@refs/heads/main
触发人      : popfbi-bot

请核对上面这个仓库，必须就是你填 Secrets 的那一个。
fork 不会继承上游仓库的 Secrets，填到上游等于没填。

================================================================
② 本仓库 Variables 名字清单（不含值）
================================================================
全部为空：这个仓库一个 Variable 都没建。

================================================================
③ 常见 Secret 名探测（只报存在与否与长度，不含值）
================================================================
   EMAIL_SENDER         邮件发件人地址                  已设置，长度 13
   EMAIL_PASSWORD       邮件 SMTP 授权码              已设置，长度 16
   EMAIL_RECEIVERS      收件人                      已设置，长度 28
   EMAIL_SENDER_NAME    发件人显示名                   空 / 不存在
   EMAIL_USER           邮件账号（别名写法）               空 / 不存在
   EMAIL_PASS           邮件密码（别名写法）               空 / 不存在
   EMAIL_PASSWD         邮件密码（别名写法）               空 / 不存在
   EMAIL_AUTH_CODE      授权码（别名写法）                空 / 不存在
   SMTP_PASSWORD        SMTP 密码（别名写法）            空 / 不存在
   MAIL_PASSWORD        邮件密码（别名写法）               空 / 不存在
   GEMINI_API_KEY       Gemini API Key           已设置，长度 53
   GEMINI_API_KEYS      Gemini Key 多枚写法          空 / 不存在
   GEMINI_KEY           Gemini Key（别名写法）         空 / 不存在
   LLM_GEMINI_API_KEY   LLM 通道的 Gemini Key       空 / 不存在
   GOOGLE_API_KEY       Google API Key（别名写法）     空 / 不存在
   OPENAI_API_KEY       OpenAI Key               空 / 不存在
   DEEPSEEK_API_KEY     DeepSeek Key             空 / 不存在

有名字被读到了。请把带「已设置」的行名，和本项目 workflow 实际使用的名字对齐：
   本项目读取的是 EMAIL_SENDER / EMAIL_PASSWORD / EMAIL_RECEIVERS / EMAIL_SENDER_NAME
   名字不一致时，即使值填了也不会被使用。

================================================================
④ 结论与排查顺序
================================================================
邮件三要素已就位，可以继续看下一段输出的真实发送结果。

==================================================
发信链路实测
==================================================
EMAIL_SENDER    : 326***@qq.com
EMAIL_PASSWORD  : 已读到，长度 16
EMAIL_RECEIVERS : 值长度 28，按英文逗号切分得到 1 个收件人
EMAIL_SENDER_NAME: (未设置，用默认值)

⚠ 已定位问题：EMAIL_RECEIVERS 的值里含有换行符。
   本项目代码只按英文逗号 split(",")，换行不会被切分，
   整个值被当成单个收件人塞进 To 头，
   构造邮件时触发 HeaderWriteError: folded header contains newline。
   修复：多个收件人改用英文逗号分隔，例如 a@qq.com,b@qq.com

SMTP 连通测试
发件域名 qq.com -> 内置识别 smtp.qq.com:465 (SSL=True)

发送失败: HeaderWriteError: folded header contains newline: b'To: moy***@qq.com\r\nbi***@qq.com\r\n'

================================================================
⑤ 仓库 Environments 名单（判断 environment: STOCK_LIST 是否还在）
================================================================
total_count : 0
名单为空：本仓库目前一个 Environment 都没有。

注意：00-daily-analysis.yml 的 job 上写了 environment: STOCK_LIST。
      Environment 不存在时，该 job 会在启动阶段直接失败，
      表现为「每日股票分析」一跑就红、连日志都很少。
